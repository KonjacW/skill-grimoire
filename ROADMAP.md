# 差距与改进方向（维护者清单）

> **本文件的定位**：记录「当前包」与「理想中的完整工作流」之间的已知差距、影响与候选方向，供维护者与使用者判断该往哪补。
> **它不是契约**：技能行为以 `active/*/SKILL.md` 为准，本文件只描述差距与选项，不改变任何技能语义。
> **更新时机**：每次增删技能、或改完 `README.md` 之后，重跑 §5 的复验命令，把变化回写到 §2。

**最后核对**：2026-09-22（包内 12 个技能；上一版 2026-09-16）。

## 1. 覆盖度对照（工作流步骤 × 包内容）

| 工作流步骤 | 包内技能 | 状态 |
|---|---|---|
| 入口分流（直通 / 轻流程 / 正式计划） | `using-superpowers` | ✅ 在包 |
| 设计与计划 | `plan`、`spike` | ✅ 在包 |
| 并行预检与边界 | `subagent-fanout-delivery` | ✅ 在包 |
| 执行与长任务 | `plan/references/execution-loop.md`、`long-running-progress-monitoring` | ✅ 在包 |
| 实现期间的质量路径（测试驱动） | `test-driven-development` | ✅ 在包（2026-09-13 迁入）；根因定位仍无包内技能 |
| 完成前验证 | `pre-commit-verification` | ✅ 在包 |
| push 前门禁 / 独立 Review | `review-gate` | ✅ 在包 |
| 分支收尾 | `finishing-a-development-branch` | ✅ 在包 |
| 会话切换与交接 | `agent-handover-prompts` | ✅ 在包 |
| 对话内进展汇报（面向用户本人） | `conversation-progress-report` | ✅ 在包（2026-09-15 加入） |

**结论**：主干闭环完整；2026-09-13 补上「实现期间的质量路径」后**不再有整段缺失**，剩余为引用完整性与文档一致性问题。

## 2. 差距清单

每条都带可复算证据（文件:行）；影响分「外部读者」与「维护者」两种视角。

### G1 实现期间的质量路径没有包内技能（影响最大）

- **证据**：`active/plan/references/deliverable-task-schema.md`（「TDD 细节由 `test-driven-development` 定义」）、`active/pre-commit-verification/SKILL.md`（`related_skills` 与正文均引用 `test-driven-development`）—— 而该技能**不在包内**。
- **影响**：读者拿到「完成前怎么验证」，但拿不到它的**上游**：测试怎么写、失败怎么定位根因。
- **候选方向**：① 只补测试驱动那一个技能（若其来源与许可清晰、frontmatter 合规，改造量最小）；② 连同根因定位一起补；③ 不补技能，改为在包内写明「本包不含该技能，按本节文字自行执行」的降级说明。
- **成本**：① 低；② 中（正文含宿主专有表述，需逐处中立化）；③ 低。
- **状态（2026-09-12）**：已按候选方向 ③ 处理——`plan` 与 `pre-commit-verification` 正文内联了 TDD 判据（先写失败测试→最小实现→重构；验证「测试是与代码同时写的」而非事后补），并删除了全部指向 `test-driven-development` 的派发式引用（`related_skills` 与祈使句）。因此本条目不再是「悬空引用」，只剩「包内没有独立的测试编写/根因定位技能」这一**能力缺口**；要补仍按方向 ①。
- **2026-09-13 已按方向 ① 执行**：`test-driven-development`（含 `references/writing-good-tests.md`）已迁入 `active/` 并进包；按「一个技能只有一份可发现副本」的规则，另一宿主技能树里的同名旧副本已以 `SKILL.md.disabled` 退役（`references/` 保留作回滚锚）。根因定位（`systematic-debugging`，15 KB、含宿主专有表述）**仍未迁**，理由是没有实测的缺口证据。
- **备注**：这一条曾被维护者主动暂缓（「待按个性化理解再充实」），本文件的 §4 保留了它需要先回答的问题。

### G2 `github` 不在包内，而 `pre-commit-verification` 明确拿它做对比

- **证据**：`active/pre-commit-verification/SKILL.md` 的 `related_skills` 与正文对比句（「本技能验证**你自己**的改动；`github` 审**别人**的 PR」）。
- **影响**：外部读者的对比落在一侧空白上。
- **候选方向**：① 把 `github` 加进 SOURCES 白名单（注意它会带入 `references/` 与 `scripts/` 多个文件，需过脱敏门禁）；② 把对比句改成自足表述（不指向包外技能）；③ 保持现状。
- **成本**：① 中；② 低。

### G3 `deliverable-checker-suite` 不在包内（**最轻**，已有降级说明）

- **证据**：`active/subagent-fanout-delivery/SKILL.md` 的 `related_skills`；同文件正文**已写明**「这些技能缺失时，按本技能正文的重叠分级与任务卡执行」。
- **影响**：低（读者有明确 fallback）。
- **候选方向**：① 补进包；② 什么都不做（推荐）。

### G4 `sketch` 不在包内，而 `spike` 引用它

- **证据**：`active/spike/SKILL.md` 的 `related_skills`。
- **影响**：低（`spike` 的动作不依赖它）。
- **候选方向**：① 补进包；② 从 `related_skills` 里去掉（前提：正文确实不需要）；③ 保持现状。

### G5 README 存在**事实过期**（对读者最直接）

- **证据**：`README.md` 正文写「**八个**技能」（实为 9）；同段写「**不含可执行代码**、不依赖第三方库」（但新增的技能带两个 PowerShell 脚本并调用系统工具）；「## 技能一览」表只有 8 行，缺新增技能。
- **影响**：读者先读 README，这三处会直接误导。
- **候选方向**：① 逐处更正 + 表格补行；② 更彻底：把「技能一览」表改成从 `active/` 自动生成的清单（维护成本最低）。
- **成本**：① 低；② 低-中。
- **注意**：`README.md` 不在 SOURCES 白名单里，属仓库根文件，**手改**；改完仍需跑发布流程让门禁放行。
- **计数已自动化**（2026-09-13）：`active/` 目录数 × README 一览表行数 × README 正文声明 × ROADMAP 全部声明 由 `publish.py` 的 O5 门禁四方比对，失败即拒绝推送。**不要再手抄这些数字**——本批第一次修改就在 §2 漏掉了一处旧计数声明（N=9 而实际 10），正是手抄造成的。注意：门禁按字面匹配「包内 N 个技能」，所以**叙述性引用旧计数时请写成 `N=9`，不要复述原句式样**，否则你自己的说明文字也会把它拦下。

### G6 维护者的工作流规则未随包发布（口径可能分裂）

- **证据**：维护者的私有设计文档里有四条规则（汇报结论的可信度分层、开工前的预算与止损线、开工前的环境就绪检查、技能面注入成本口径），而包内 N=11 的技能与 README 的「工作流全景」都没有对应节点。
- **影响**：外部读者按包执行时拿不到这四条；维护者自己也可能出现「文档一套、包一套」的口径分裂。
- **候选方向**：① 分别落到最贴近的包内技能；② 在 README 加一节「工作流规则」集中说明；③ 明确「私有文档是原理面、包是执行面」，并在 README 里声明这一分工（推荐先做 ③，再决定是否 ①）。
- **成本**：① 中；② 低；③ 低。

### G7 设计意图文档不在包内

- **证据**：`active/using-superpowers/SKILL.md` 里指向设计意图的那一行，对**外部读者**是一个他拿不到的指针。
- **影响**：包可以**用**，但读者无法知道「为什么这样设计、边界在哪」。
- **候选方向**：① 从私有设计文档抽一份**对外版**（去掉宿主矩阵、本机路径与历史包袱）放进包根；② 扩充 README 的「设计取向」一节；③ 保持现状，接受那行是「对作者自己的指针」。
- **成本**：① 中；② 低。

### G8 包自身缺少「内部引用可解析」自检（这类漂移会复发）

- **证据**：G1–G4 的悬空引用能长期存在，是因为 `scripts/publish.py` 的门禁只查脱敏 / frontmatter / 编码 / 宿主专有写法，**不查** `related_skills` 与反引号路径能否在包内解析。README 的三处过期同样无人拦。
- **影响**：每次增删技能都可能再漂一次。
- **候选方向**：① 在 `publish.py` 的 `check()` 里加一条规则：扫 `active/**` 的 `related_skills` 与反引号路径，目标不在 `active/` 内即报 **warn**（不 fail，避免锁死）；② 只写成维护者手检步骤；③ 不做。
- **成本**：① 低-中（改脚本 + 自测 + 复发布）；② 低。
- **状态（2026-09-13）：已按方向 ① 实施，且判据比原方案更严** —— `publish.py` 的 O5 门禁做两件事：① 悬空引用（`related_skills` + 反引号 `技能/references|scripts/...`）目标不在 `active/` 内即 **ERROR**（不是 warn：G2–G4 的历史证明悬空引用会长期存活；刻意的包外引用走 `KNOWN_EXTERNAL_REFS` 白名单，当前只有 `deliverable-checker-suite`，不会误锁）；② **计数四方比对**（`active/` 目录数 × README 一览表行数 × README 正文声明 × ROADMAP 全部声明）。两个实测坑已固化进实现与注释：**只匹配第一处会假阴性**（第一次改动就漏了 §2 里一处旧计数（N=9）而门禁照过）；**叙述性复述旧计数会把维护者自己的说明文字一并拦下**（失败信息里已带约定：引用旧计数写成 `N=9`）。

### G9 小项

- `README.md` 的「目录结构」一节只列了 `active/<skill>/SKILL.md` 与 `references/`，**没提技能内可以带 `scripts/`**（新增的技能就有）。**本批已修**（补了 `active/<skill>/scripts/` 一行）。
- 包内无 CHANGELOG / 版本注记（变更只在 git log 里）。
- 已核实**无需改**的一条：README 说「技能内容与具体仓库无关」——新增技能的示例路径在发布副本里已被脱敏为占位符，该表述成立。**该条记录在此是为了防止下次被误改。**

### G10 脱敏的「整行规则」会静默塌缩：两条内容不同的 live 行被同一条规则覆盖，其中一条整段丢失

- **证据**：live 副本 `~/.codex/skills/active/agent-handover-prompts/SKILL.md:69`（123 字，陷阱教训「照抄规范样本会漏启动语句」）与 `:80`（239 字，规范样本指向）**内容不同**，却被同一条 `line_rule` 命中，替换文本同为一句 83 字；结果发布副本 `active/agent-handover-prompts/SKILL.md:69` 与 `:80` 是同一句 83 字，第 69 行那条教训**零残留**。重跑两次发布 sha1 不变——塌缩是幂等的，所以门禁不会报警。
- **影响**：发布副本静默掉内容；因为幂等，检查不会发现，读者也看不出少了什么。
- **候选方向**：① 把「同一条 `line_rule` 命中 ≥2 条**内容不同**的 live 行、且替换文本不含回引/分支」做成发布流程的**静态门禁**（fail 或 warn）；② 只靠维护者人眼复核（=现状，已经漏过一次）。**不要**改用「有没有重复长行」这类文本启发式兜底：live 里本就存在两条合法的重复长行（`finishing-a-development-branch:99,158`、`plan:158,170`，均为英文样板行），启发式必然误报。
- **成本**：① 低-中（改 `scripts/publish.py` + 自测 + 复发布）；② 零。
- **状态**：**已修**——私有脱敏表新增一条只匹配第 69 行的规则；Temp 目录重跑实测：两行各自保住、0 重复长行、门禁 exit 0。**已复发布**：包内 `active/agent-handover-prompts/SKILL.md` 的 `:69`（陷阱教训）与 `:80`（指针）内容各异。

### G11 `scripts/` 目录同时绕过脱敏与门禁（潜在泄露面）

- **证据**：`SKIP_DIRS = {".git", "scripts"}` 在改前位于 `scripts/publish.py:38`（改后 `:46`），被 `iter_text_files()`（改前 `:155`，改后 `:184`）按**目录名**在**任意层级**套用 → `active/<技能>/scripts/**` 既不参与宿主中立化，也不进 `--check` 扫描。实测本仓 27 个文本文件只扫到 23 个，漏掉的正是根 `scripts/` 下 2 个文件加 `active/long-running-progress-monitoring/scripts/` 下 2 个 `.ps1`。
- **影响**：技能内脚本是唯一不受脱敏/门禁约束的发布内容。当前两个 `.ps1` 逐类扫描 0 命中（无私人内容），属**潜在**泄露面：一旦技能内脚本带上私人路径，发布流程不会拦。
- **候选方向**：① `iter_text_files()` 改为只跳过**根级** `scripts/`（按相对路径判定，而不是按目录名）；② 仍跳过根级，但把 `active/**/scripts/**` 显式纳入扫描；③ 不改代码，把「技能内脚本目录属于发布内容，必须过门禁」写进 §6 维护纪律。
- **成本**：① 低；② 低；③ 零。
- **状态（2026-09-12）**：已按候选方向 ① 修——`iter_text_files()` 现在只跳过**仓根第一层**的 `.git`/`scripts`（仓根 `scripts/` 仍跳过，因为那里放着被 .gitignore 忽略的私有脱敏表，门禁不能自扫），扫描覆盖 23 → 25 个文本文件，`active/<技能>/scripts/**` 已纳入。独立 Review 复核时确认：`os.walk` 不跟随符号链接，深层同名目录逃逸路径未复现。

### G12 发布副本没有「与 live 同步」门禁（本批已实际发生）

- **证据**：live 副本 `~/.codex/skills/active/subagent-fanout-delivery/SKILL.md`（85 行 / 11,294 字节，mtime 18:32）比仓内 `active/subagent-fanout-delivery/SKILL.md`（83 行 / 10,290 字节，mtime 16:58）多两行，多出的内容含「铁律 13：并行子代理若要各自起本地服务/实例，端口与清理口径必须在任务卡…」（live:31）；live `using-superpowers/SKILL.md` 49 行 vs 仓内 39 行（差的正是 G13 那两处只读档）。而发布流程 exit 0、不报错。
- **影响**：读者拿到的包比维护者实际在用的少内容，且没有任何信号；「包」与「live」可持续分裂。
- **候选方向**：① 发布流程加一条**单向漂移检查**（live→仓内 diff 非空即 warn，并打印差异文件与行数）；② 把「改完 live 立刻复发布」写进维护纪律（弱约束，靠人）；③ 接受分裂，在 README 声明「包是快照，不保证与维护者本机一致」。
- **成本**：① 低-中；② 零；③ 零。

### G13 并行判断缺「只读取证」档：只读勘察/取证类任务被推回串行

- **证据**：原并行口径（`active/using-superpowers/SKILL.md`《一次并行判断》+ `active/subagent-fanout-delivery/SKILL.md` 的 R1/R2/R3 重叠分级）只按**写入冲突 / 隔离成本**给条件 → 零写冲突的只读任务没有对应档位。实测后果：一次纯调查任务被串行执行，跑了 **18 次工具调用**（写冲突为零，集成成本仅为汇总）。live 侧现已有 B 档（`~/.codex/skills/active/using-superpowers/SKILL.md:35-41`「只读勘察 / 取证类 fan-out」：≥3 个可独立推进的只读工作包 + 各写不同产物文件 + 写权互不相交即默认分片并行），同批还给 `subagent-fanout-delivery` 加了《只读取证类 fan-out》小节（任务卡必写 6 项、主 Agent 亲自复算关键数字、「先落盘报告骨架」）。
- **影响**：只读批量（多目录勘察、多源取证、并行检索比对）白白串行，代价直接体现在工具调用次数与墙钟时间上。
- **候选方向**：① **复发布**把 live 的 B 档与新小节带进包（先做这个，包内即生效）；② 在包内重叠分级表里补一行「只读档」索引，让读者从 R1/R2/R3 表就能找到它；③ 不改。
- **成本**：① 低（走发布流程）；② 低；③ 零。
- **状态**：**已修，且已进包**——live 侧的「B 档」与《只读取证类 fan-out》小节已存在，本次复发布后包内与 live 逐文件一致（`--check` 的发布副本同步门禁为证）。它曾同时是 G12 的一个实例，现已一并闭环。

### G14 迭代式并行研究的经验未入包（2026-09-13 已处理）

- **证据**：一次多轮研究（76 次 `spawn_subagent` 调用：steer 46 行 / spawn 26 行 / list 4）暴露出的用法——**长驻子代理 + 主对话 `steer` 七类模板**（增量输入 / 纠偏 / 报错分诊+一行修法 / 红线否决 / 目标重定价+止损 / 环境广播 / 自我作废）、判据预注册、负结果跨波传递、证伪波三态结论（复现 / 不符 / 无法判定）——而包内原有正文只覆盖**终点已知**的产出类与只读取证类。
- **处理**：**并入**（不新建技能）——`subagent-fanout-delivery` 新增 `references/iterative-research.md`，正文加一节指针。理由：逐条对照后，10 条候选规则里 **0 条零重叠**（1 条完全重复、7 条部分重复、真新增 2 条 + steer 模板 + 量化刻度）；独立成技能要 +1 条技能索引并制造第二份会漂移的重复文本。
- **同时**：把并行成本刻度写进 `using-superpowers` 的《并行的成本门槛》一节（**n<3 不派**；宽度 2 的批次中位 1.73×、最低 0.84×；花费比 0.82×/0.974×/0.48× 三种口径并存；结果回灌均值 1,354 字符/条）。
- **口径出处**：维护者侧审计脚本 `audit_parallel.py`，锚定窗口 2026-09-05 → 2026-09-13 UTC（滑动窗口会让数字几分钟后不可复现，故必须锚定）。
- **已解决（2026-09-13 同日下钻）**：`steer` 相对「重派」的成本优势**已实测**——steer 单条真实边际成本中位 ≈ **$0.0010**（其中消息文本只占 5–7%，93% 是那一轮的缓存重发）；重派固定开销中位 **$0.0027**，真实子代理整单最低 **$0.0134** / 中位 **$0.064** ⇒ **重派 ≈ steer 的 2.7× / 13× / 64×**。同时补齐了它的边界：`steer` 发给**已结束**的子代理会**静默失败**（`No live subagent … spawn tree`，实测 6 会话 7 处），且 `steer` 是单目标（「广播」= 逐个各发一次）。

### G15 门禁自身的遗留项与盲区（低，2026-09-13 由第一套测试与变异检验发现）

- **门禁不扫仓根 `scripts/`**（因为私有脱敏表就放在那儿，门禁不能自扫）：所以 `scripts/test_publish.py` 内的字符串**不受门禁约束**。当前测试文件用的是拼接构造（不写字面量），但这是一条需要维护者自觉的盲区——**别在 `scripts/` 下放私人路径/项目名/实名**。
- **`REPLACEMENTS` 缺「单反斜杠」路径档**：文档里若直接写 Windows 原始路径（单反斜杠），`scrub()` 不动它，只能靠「本机绝对路径」规则**拦下**（fail-closed，不会泄露，但把「可自动归一」降级成「人工修完再发」）。同仓私有表里 `<PROJECT_DIR>` 那条反而有单反斜杠写法，台阶自身不一致。
- **多处裸 `open(...).read()` 不关闭**：`read_text`、`load_line_rules`、`path_replacements`、`check_doc_consistency`、`sync`、`check_line_rule_collapse` 都有；跑测试会刷 `ResourceWarning: unclosed file`（不影响判定）。
- **测试断言耦合中文报错文案**（T3/T9/T10 等）：这是刻意的判别力，但**改门禁文案时必须同步改测试**。
- **已修（2026-09-14 第二次提交；经第二轮独立 Review 复核）**：① `related_skills` 取值不剥引号导致**合法写法被误报** → 新增 `_related_skill_names()`（T19）；② 计数只比行数不比名字 → 增加**名字集合比对**（T18）；③ README 侧「全部声明」缺护栏 → 补 T17；④ 门禁的修法建议与自身判据自相矛盾 → 阿拉伯/中文两种写法都接受；⑤ 「技能一览」小节缺失时报「表 -1 行」→ 改为明确提示；⑥ T14 夹具不覆盖真崩溃形态 → 改为「一个可解析 + 一个不可解析」。
- **第二轮 Review 的追加修正**：⑦ **T17 第一版是空转用例**（夹具写成「本包共 2 个技能。」，数字与「个技能」之间有空格 → 该声明根本没被匹配，变异下仍绿）→ 夹具改紧接写法，并补 T20（带空格的叙述句**不得**被当声明）/ T21（阿拉伯数字声明可接受）/ T22（缺小节只报一条；小节在文末不算缺失）；⑧ `_related_skill_names()` 还有四种合法 YAML 写法会 fail-open 或误报（块内空行、块内注释行、行尾注释被吞进名字、跨行 flow 写法整表丢失），另加「正文散文里的同名键被当声明」→ 全部修掉并加 T23 四条断言；⑨ 名字比对在小节缺失时逐技能刷「表缺 X」噪声 → 加 `seg is not None` 守卫；⑩ 第五处计数（`SOURCES` 条目数）无护栏 → 并入同一比对；⑪ README 里那三个读取次数原为**不可复算**口径 → 改为「提到 `SKILL.md` 路径的消息数」并标注快照日期（F-7）。
- **已知残留（接受，未修）**：① `_related_skill_names()` 不处理 YAML 折叠/多行字符串与键名大小写变体（本仓不出现；要完备必须引 `yaml`，而发布脚本刻意零依赖）；② 名字比对沿用「表行必须是 `| \`` + 名字 + `` ` ``」的紧格式（写成 `|\`plan\`|` 这种不带空格的竖线会被判「表缺」）；③ 在表里**预告尚未进包的技能**会被判「表多出」——这两条都是 fail-closed 的风格约束，作者按现行格式写即可，不打算放宽（放宽会削弱判别力）。

### G16 门禁跳过仓根 `scripts/` ⇒ 该目录里的本机路径永不被扫（G11 的残余；2026-09-14 修）

- **证据**：公开仓里 `scripts/publish.py` 命中维护者用户名 **15 处**（`CODEX = "…/.codex/skills"`、`HERMES = …`，以及替换表里 12 行盘符路径字面量），随公开仓一路发布；而同一时刻 `publish.py --check` 报「无私人路径」。根因：`iter_text_files(REPO)` 按设计只跳过**仓根第一层**的 `.git/` 与 `scripts/`（本机私有脱敏表就放在 `scripts/` 下，门禁不能自扫自己的词表）⇒ 该目录既不被脱敏、也不被扫；G11 只把 `active/<技能>/scripts/**` 纳入扫描，**仓根 `scripts/` 仍在外**。
- **影响**：维护者用户名与目录结构在公开仓可见（不是密钥，但正是门禁存在要防的那一类）；且该目录内容不受任何门禁约束。
- **处理**：
  1. `publish.py` 的本机路径**全部改为从环境推导**（`~` / `%LOCALAPPDATA%`），替换对由 `_home_path_pairs()` 生成——顺带让克隆者也能跑（原先硬编码维护者路径，克隆侧必然跑不通）；同时补上 G15 记过的「单反斜杠」替换档。
  2. 新增 **O6 门禁** `check_scripts_leak()`：把「泄露类」规则（本机绝对路径 / 疑似密钥 / 私人项目名 / 私人路径 / 实名 / 服务器凭据）单独套到仓根 `scripts/` 的文本文件上；豁免 `*.local.json`（私有表按设计就装着私人词表）；**不套「宿主专有工具」规则**——该文件的职责就是定义「宿主专有工具名 → 中立名」的替换与禁则，它必须写出那些名字才能替换它们。
  3. 测试 24 → **27**（T4 改为断言四种写法全归一 + 改用另一盘符做 fail-closed；新增 T25/T26/T27），并做 **5 条变异检验**（摘 O6 接线 / 去私有表豁免 / 还原 `_win_spellings` 旧 bug / 把宿主专有工具混进 `LEAK_CATEGORIES` / 只扫 `.py`）——全部变红，未变异的对照夹具全绿。
- **状态（2026-09-14）**：已修。`--check` rc=0、`Ran 27 tests OK`；真仓负对照（往 `scripts/` 注入一条本机路径）rc=1 并报出 `本机绝对路径 | scripts/test_publish.py:558`，随后字节级还原（sha256 一致）。
- **未做（登记）**：**O7** —— README 声明的测试用例数仍靠人工同步（本轮 24 → 27 是手改的）。把它门禁化需要「README 数字 vs `def test_` 实计数」比对 + 用例 + 变异检验，本轮未纳入，避免把改动面扩到与泄露修复无关的第二件事。


### G17 对话进展汇报的落点、命名与「自动触发」的落地方式（2026-09-15 加入技能时登记）

- **背景**：`conversation-progress-report` 于 2026-09-15 进包。触发方式是**自动启动**：对话出现第一个**阶段成果**（完成一个阶段 / 关键结论 / 实验出数 / 口径定版）就自己开，不等用户口令；「开汇报 / 看汇报 / 关汇报」只作手动控制面。更新是**纯事件驱动**——不设轮数或时间兜底。
- **为什么必须这么做**：两个宿主都**没有条件匹配器**（索引里只有名字 + 描述 + 路径），所以「自动」只能靠每轮自动出现在上下文里的东西：描述里的症状词、宿主常驻场景表、以及 Hermes 侧的 hook 注入。
- **Hermes 侧机制**：`pre_llm_call` shell hook + 本技能的 `scripts/pre-llm-reminder.py`（会话消息 < 8 条不注入，之后每新增 6 条注入一次；每轮写心跳状态文件，供「关汇报」定位当前会话）。复验：`hermes hooks doctor`（须 `✓ allowlisted` + `✓ script unchanged since approval` + `✓ produced valid JSON`）。
- **Codex 侧机制**：没有 hook，只能靠宿主 `AGENTS.md` 场景表的**症状条目**（写口令没用）。本仓 README 的装配片段已改为症状措辞。
- **落点与命名（未定版）**：写进维护者的 Obsidian vault（`report/` 目录，公开副本里按脱敏表替换为 `<OBSIDIAN_VAULT>`）；默认命名 `<对话主题>_进展汇报_<YYYYMMDD>.md`（同一对话同一天编辑同一个文件，覆盖式、顶部带更新时间）由维护者**暂定**，技能正文已写明「以用户后续规范为准」，故此处不设门禁。
- **边界（已写进技能正文）**：本技能只管**当前这一场对话**；`report/` 下《长对话进展 · 最新成果》这类多会话横切汇总由另一条链路产出，两者不合并。
- **口径更正（2026-09-16 复发布时发现，必须读）**：**「自动启动」已被取代**。live 的 `scripts/pre-llm-reminder.py` 于 2026-09-15 15:33 改为**口令制**——**默认不写任何 report 文件**，只有用户在本会话说「开汇报」（或等价明确指令）后 agent 才维护，且只在出现实质进展时更新；脚本靠 `<HERMES_HOME>/progress-report-on/<session_id>.flag` 判定是否启用，没有该标记即一个字都不注入。本 G17 上面那段「出现第一个阶段成果就自己开」是更早的形态，**不要再据此实现**。该改动此前未复发布（发布副本落后 live），本次随 G18 一并进包；否则门禁的「发布副本落后 live」检查会失败。

### G18 运行期触达层缺失：技能是「被动文档」，Hermes 侧没有任何症状表（2026-09-16 登记并修复）

- **报的症状（用户原话）**：① 很少见到它被调用 ② 很少并行化 ③ token 开销依旧很多、执行任务时间依旧很长 ④ 不能根据情况适时调用发散探索流程。
- **根因（实测，非推断）**：技能索引里只有「名字 + 描述 + 路径」，触发条件写在技能正文里；而 Hermes 侧**没有任何常驻症状表**——根级 `AGENTS.md` 不存在（`agent/subdirectory_hints.py` 拒绝工作目录树以外的路径，源码原文：「subdirectory hints reject paths outside the working dir so `~/.codex/AGENTS.md` / `~/.claude/CLAUDE.md` never mix in」），持久记忆也已满（3,291/3,300 字符）。⇒ 触达只能靠**用户消息里的词 × 索引描述里的词 × 记忆指针**三者对齐。
  - 逐条取证：`state.db` 全部 666 会话的 `source` 只有 desktop/cron/tui/unknown——**一条 Codex 会话都没有**，所以「Codex 侧那张表让本仓技能被读」这个解释不成立（本仓 G18 之前的说明曾这么写过，此处更正）；读 `review-gate` 前最近的用户消息原文是「请进行独立 review…」「…按 review-gate 技能规定的格式…」「好的，请进行 push」；`MEMORY.md` 有 `review-gate` 指针、没有 `using-superpowers` / `subagent-fanout-delivery` 指针。
- **实测基线**（14 天，窗口 `since_utc = 2026-09-02T10:27Z`；口径 = 调用数按 assistant 行 `tool_calls` 条目，见 `scripts/audit_trigger.py` 的 docstring。**基线 JSON 是维护者本机产物、不在仓内**，故需要的字段都就地写在本条里）：
  - grimoire 11 技能被读 **158 次 / 全部技能读取 562 次**；其中 `review-gate` 53、`agent-handover-prompts` 41、`subagent-fanout-delivery` 19、`plan` 15，而**路由入口 `using-superpowers` 只有 4 次**（其中 2 次发生在「任务本身就是改技能包」的会话里）。
  - `spawn_subagent` **393 次，覆盖 48 / 565 个会话**；其中「只派 1–2 次」的会话 17 个（串行派发，不是分片并行）。
  - 成本：`skill_view` 返回体只占全部工具输出的 **5.9%**（terminal 38.8% + read_file 34.0% 才是大头）。**>10k 字符的异常读取**（同一 14 天窗口）共 **177 条：包外技能 156 条**（最大 95,211 字符）、**本仓技能 21 条**（`plan` 15 / `pre-commit-verification` 4 / `test-driven-development` 2，量级 10.4k–11.8k 字符）。**复算方式（自足，不依赖维护者私有产物）**：`python scripts/audit_trigger.py --days 14` → `M3.skill_view_body.over_10k_by_skill`；归属用 tool 结果行的 `tool_call_id` 与 assistant 行里每个 tool_call 的 `id` 配对，**人工「按相邻行猜」会归错**（本仓第一次就错记成 22 条、并把 `subagent-fanout-delivery` 记成 4 条，实为 0 条——这正是本条被独立 Review 两次盯上的原因）。⇒ **「>10k 的异常读取全部来自非本仓技能」不成立**；量级差一个数量级，**结论方向不变**：本轮解法是让技能减少工具调用次数与串行等待，不是让技能变短。
- **处理（三层，2026-09-16 落地）**：
  1. **索引层**：11 条 `description` 改成「可观察症状 + 动作」。**口径（唯一）：去掉 `description:` 前缀与引号后的纯文本值、取自仓内发布副本 ⇒ 537 → 441 字符（−96）**；索引是唯一每轮都付的成本，只减不增。**不要再用「702 → 606」**——那是 live 侧「含前缀与引号的行长」，旧值取自另一棵树，两个数不同源会让人无法复算。
  2. **注入层**：新增 `active/using-superpowers/scripts/workflow-trigger.py`（`pre_llm_call` hook，条件注入）：命中才注入 1 行（**53–119 字符，均值 83**；只含技能名与动作，**不含任何路径**——最长那条原含 `.md` 相对引用，2026-09-16 已换成技能名）、零命中回 `{}`、**同一技能每会话至多 1 次**（用户 2026-09-16 裁决 A1）、子代理回合静默、`--selftest` 内联 **24 项用例**、零第三方依赖。
     * **为什么从「12 条消息冷却」改成「每会话一次」**：注入文本会写进该轮 user 消息的 `api_content` 旁路（`agent/chat_completion_helpers.py:1957` 注释：api_content = "the exact bytes the main loop sent"），并被之后**每轮重发**——压缩器里那一族 `drop_stale_api_content()` 的存在理由就是「replay cannot resend stale bytes」。按消息数冷却会让常驻成本随会话长度**线性**增长（实测口径 n×48 字符 = 7 条规则 × n/12 次 × 83 字符），改成每会话一次后**上界是常数**（7 × ≈83 ≈ 581 字符）。
     * **实测（token 驻留证据）**：一个会话被注入 3 轮，每轮 user 行的 `api_content` 比 `content` 长 **+72 字符**（= `push_gate` 行 70 字符 + 宿主拼接的 `\n\n`；口径：user 行、2026-09-16 19:5x）；全库 `api_content` 含该标记的条目 **28 条 / 18 个会话**（2026-09-16 20:1x 快照；首测 19:5x 为 23 条，数字随会话增长——引用必须带时刻）。（`request_dump_*.json` 未开启，故驻留判定用「源码 + 库内 sidecar」两级，未拿到线上字节。）
     * **已知限制/不变量（有用例钉死，勿无声改）**：**坏状态文件不抛异常**（`main()` 兜底 + `decide()` 类型守卫）；**静默分支保留注入记录**（清空会让下一次「无 parent」的调用重复注入）；旧状态文件的 `last` 字段按**键名**兼容为「已注入」；**无 `session_id` 时不注入也不落盘**（无法按会话去重，宁可沉默——共享的 `unknown.json` 会把该技能对之后所有无 id 调用永久封死且不自愈）；**先交付再落盘**（进程被杀/宿主超时时不留「已注入」误记；插件侧做不到这个次序，残留已在插件里写明并接受——热路径 ~1.9 ms vs 默认超时 30 s）。
     * **取舍（A1 的代价，必须一起读）**：改成每会话一次后，**C 档发散类与其余规则在长会话中途无法再提醒**（旧语义下 12 条消息后可以再提醒一次）。这是用「少提醒」换「常驻上界常数化」，不是白拿的收益。**降级形态与解除条件**：若「中途再提醒」被证明必要（例如长会话里主 Agent 反复错过并行窗口），退回「按轮数窗口 N」即可——机制（注入准入）不变、只换窗口口径；届时需同步改 `decide()` 的准入判据与用例，并把上界重新算一遍。
  3. **度量层**：新增 `scripts/audit_trigger.py`（只读 `state.db`，仓内不发布）+ `scripts/test_audit_trigger.py`（5 用例 + 口径自检 `M0`）。
  4. **交付方式（2026-09-16 追加）：本机改用进程内插件**。shell hook 每轮要起一次 python 进程，实测中位 **196 ms/轮**（口径：**解释器 = `which python` 指向的 hermes-agent venv 解释器**；python `subprocess.run` native 侧计时、8 次取中位、测量于 2026-09-16 19:3x；**并发负载会抬高它——第三轮复核在同一台机上并发跑 E2E 时量到裸解释器 197–274 ms**，故绝对数应读作量级）；拆开 = 裸解释器 **84 ms** + stdlib import **≈80 ms** + 脚本工作 **≈15 ms**。⇒ 真正的成本是**进程启动**，不是判定逻辑。故新增机器本地插件 `$HERMES_HOME/plugins/grimoire-trigger/`（`plugin.yaml` + `__init__.py`，`register(ctx)` → `ctx.register_hook("pre_llm_call", …)`），它用 `importlib` 加载本技能包里的 `workflow-trigger.py` 并调其 `decide()/render()`、共用同一状态目录。**规则与判定仍然只有技能包里那一份**（避免第二份会漂移的副本）。
     * **实测**：插件进程内开销 **1.57 ms/次**（首次 49.7 ms 含加载脚本；测法：临时 `HERMES_HOME` 下直调 200 次取均值）⇒ 相对 shell hook **≈125×**。`hermes plugins doctor grimoire-trigger` 无 WARN；`hermes hooks doctor` 只剩 1 条（进度汇报）。
     * **端到端（新进程探针，2026-09-16 20:01）**：注入成立（状态文件 `{"n":27,"injected":["subagent-fanout-delivery"]}`，由插件写入）+ agent 读了该技能 + 3 份产物落地；**但这次它没派子代理**（自己串行做完，12 次工具调用）——与 W3 改后组那次「3 路同批」不同 ⇒ **「注入 → 读技能」稳定，「读技能 → 真的分片并行」还有方差**，属下文《同一验证暴露的另一个问题》同一类缺口。
     * **未做/待办**：插件暂为**机器本地**，未进公开包（`SCRIPT` 硬编码了本机 live 路径；要发布给其他 Hermes 用户需先把它参数化，否则在别人机器上会静默失效）；桌面端**新进程**才生效（当前运行中的后端仍用旧路径）。
- **执行中发现并修掉的三个真问题**（都有 0 依据的猜测被实测推翻的记录）：
  1. **双重计数**：一个逻辑调用在库里是两行（assistant 行 `tool_calls` JSON + tool 结果行 `tool_name`），两边都数会整整翻倍。**同一指标在不同时刻的快照必须带测量时刻**：`spawn_subagent` **390 vs 390**（2026-09-16 16:23）、**393 vs 393**（冻结基线窗口 `since=2026-09-02T10:27Z`，2026-09-16 18:00）、**400 vs 400**（2026-09-16 19:16）。第一版脚本与最初的手算都踩了双重计数；现已由「总数断言」用例守住（变异检验已证明它变红）。
  2. **`min_msgs` 挡住最该触发的形态**：发散类原本要求「对话 ≥8 条」，结果**新会话第一句就提开放目标**（n=0）恰好被挡。改为「消息成形（≥16 字符）**或** 会话已聊开（≥8 条）」的 OR 门槛，并加「首轮长句也注入」用例守住。
  3. **子代理静默失效**：`parent_session_id` 被 Hermes 提升为**顶层** payload 键（`agent/shell_hooks.py` 的 `_TOP_LEVEL_PAYLOAD_KEYS`），只读 `extra` 会拿到空串 ⇒ 子代理回合照常注入。已改为两处都读（顶层优先）。**残留（实测，2026-09-16 18:5x 更新）**：子会话的**首次** LLM 调用仍可能读到空 parent。直接观测：被注入的子会话 = `20260916_182822_e2090b`（W3 A 题子代理）、`20260916_184001_a22d89`（Review 子代理）；静默的子会话 = `20260916_182912_673569`（W3 B 题子代理）。⇒ **不是每次发生**（样本小，不给百分比）。**观测事实（可复算）**：live 状态目录里**子会话的状态文件 n 恒为 1**——hook 对每个子会话只被调用一次（审查子代理那个文件 `20260916_184001_a22d89.json` 的 mtime = 18:40:02，至 2026-09-16 19:16（36 分钟）未再更新）⇒ 上限明确：**每子会话最多多注入一行**。⚠ 「首次调用读到空 parent」是**与观测相容的解释、未取证**；根治方向应是先查清「子会话后续回合为何不再触发 `pre_llm_call`」，而不是继续猜 parent 键。量级有界，**不为此加投机性兜底**。
- **W3 行为验证（同题改前/改后各 4 个一次性会话，`hermes chat -q … --oneshot`；事实取自 `state.db`，不看自述）**：

  | 题 | 改前 skill_view / 派发 | 改后 skill_view / 派发 |
  |---|---|---|
  | A 只读清点 3 个目录（各写一份清单） | 0 / 0 | **1（subagent-fanout-delivery）/ 3 路同批** |
  | B 明说「并行拆给几个子代理」 | 1 / 1 | **2 / 3 路同批** |
  | C 开放目标「优化启动耗时」 | 0 / 0 | **1（using-superpowers）/ 0** |
  | D 「都改好了，准备收尾」 | 0 / 0 | **2（pre-commit-verification）/ 0** |

  ⇒ 触达 1/4 → **4/4**；显式并行的派发从 1 路 → **3 路同批**。
- **同一验证暴露的另一个问题（不属本条范围，登记待办）**：改后组 A/B 的子任务在日志里被标 `✗`，且**产物清单文件未落地**（`after-A-*.md` 不存在；改前组单干时是落地的）。子会话消息在关闭后被回收（实测：消息数由 5 变 0），故只能从文件系统判定。⇒ 并行把「落盘」这一步丢了，正好落在 `subagent-fanout-delivery` 的任务卡必写项上；后续应在该技能的任务卡模板里把「产物路径 + 落盘后回读校验」写成硬项。
- **独立只读 Review（2026-09-16，review-gate 中风险档；审查者只读、只认集成后最终 diff）**：verdict = **有条件可 push，无阻断级 finding**（3 重要 + 7 次要），处置如下：
  1. **【重要·已回退】实验副作用污染了被测技能正文**：W3 的 D 题一次性会话在「验证」过程中对**被测技能自身**执行了 `skill_manage(patch)`，给 `pre-commit-verification` 的 `## Pitfalls` 加了 3 行（live mtime 18:33:09；来源会话 `20260916_183131_7d6aa1` 在 18:33:06 的 `tool_calls` 可查），并随 18:38 的复发布进包——违反「技能正文一个字不改」。**已从 live 回退并复发布**（回退后该技能相对基线只剩 `description` 一行；被移除的原文留在维护者本机 Temp，未擅自收编）。**教训**：行为验证用的一次性会话不得对被测对象落盘；这类驱动脚本（如 `run_w3.sh`）必须在跑完后对 `active/` 断言 `git status` 无改动。
  2. **【重要·已更正】假断言**：见上文成本项——「>10k 的异常读取全部来自非本仓技能」被证伪（实为**本仓 21 条 / 包外 156 条**，第二轮复核用 id 配对法独立复算与我方改写后的数字一致）。该断言曾被用来支持「砍本仓技能文本没有依据」，**结论方向仍成立但依据已换成脚本可产出的 `over_10k_by_skill`**。
  3. **【重要·已登记】轻量化承诺与交付物不符**：计划文档 §1.5 写「技能正文一个字不改」，而公开副本另带两处**存量正文漂移**（都是 09-15 的 live 变更、由复发布带入，非本次语义改动）：`agent-handover-prompts` 新增《交接触发门禁》节（09-15 09:11，来源会话 `20260915_080106_1ba623`）与 `conversation-progress-report` 的触发段改口令制（后者已在 G17 更正里登记）。**本条即 handover 那节的登记**，此后这两处不再是「未声明的正文变更」。
  4. **【次要·已修】**：① `workflow-trigger.py` 遇坏状态文件曾抛异常（rc=1、该次注入静默丢失）⇒ `main()` 加兜底 + `decide()` 类型守卫 + 4 条用例，两个变异检验都变红，真实载荷端到端 rc=0；② 描述总长、注入字符数、`spawn_subagent` 计数三处口径统一（见上文）；③ `scripts/audit_trigger.py` 的 docstring 收窄为可复算形式（两族逐会话相等但全库不等、另有通用名 `tool_call` 条目、两套口径不可混用）——**具体数字见 docstring 的 2026-09-16 快照，本条不再复述**（复述正是前几轮数字漂移的来源）。
  5. **【次要·留用户裁决】每轮新增成本**：新 hook 是**每轮都付**的固定成本——**单次进程 spawn 0.22–0.35 s/轮**（本机口径：`hermes hooks doctor` 报 0.203–0.219 s；含宿主 wrapper 的一次 `time` 测量为 0.335 s；纯 python 起进程独立测量 7 次均值 0.223 s）**+ 每轮写一次状态文件心跳**（零命中时也写）。按用户「每轮都付的成本只许减不许增」的字面口径，这需要显式申报与裁决；可选退路：把注入并入已有的 `pre-llm-reminder`（同一次 spawn 做两件事，省掉一次进程）或改成非每轮触发。
- **第二轮只读复核（2026-09-16，审 `50cf1e9..c422811`）**：verdict = **有条件可 push**（无阻断；1 条重要 + 6 条次要）。处置：① **重要·分解仍不可复算** ⇒ 已把「按技能分解」做进 `scripts/audit_trigger.py`（`skill_body_breakdown()` + 用例 + 变异检验，归属改用 `tool_call_id` 配对），数字改为脚本产出并就地写入口径与复算命令（上文成本项已更新；**人工按相邻行归名会把技能算错**，这是连续两轮被盯上的根因）；② **次要·静默分支清空冷却历史**（我在上一轮修复里引入的回退，`return None, {}`）⇒ 已改回保留记录（**当时字段名是 `last`；A1 之后已改名 `injected`**，用例同步改名为「子代理回合静默且保留注入记录」），并加该用例（先红后绿 + 变异检验）；③ 注入行「不含路径」的断言与实现不符（`open_ended` 原含 `.md` 相对引用）⇒ 已把那条改成引用技能名，最长注入行 139 → 119 字符；④ `0.335 s` 改为区间 + 测量命令；⑤ 「40 分钟未更新」改为带时刻（36 分钟）；⑥「窗口见基线 JSON」改为就地写 `since_utc`（基线 JSON 是维护者本机产物、不在仓内，KEEP_TOP 也不允许）；⑦ 两份脚本 docstring 补取证时刻。
- **未决/未验证**：① 桌面端「是否要重启才生效」**已过期，见更正**：16:33 实测「本会话在旧 hook 状态目录有心跳、在新 hook 目录没有」，据此当时判断需重启；但 18:49 复测发现**本会话已命中 `push_gate`**（**该状态文件现为 `{"n":485,"injected":["review-gate"]}`（2026-09-16 20:05）；引用 `last`/`n=281` 的写法是 A1 之前的快照，已作废**）⇒ 触发层在当前桌面进程内已生效，中间发生了一次注册（具体触发点未取证：后端重启或宿主 plugin reload 二选一）；② 规则表与 Codex 侧 `AGENTS.md` 存在第二份语义相同的文本（与 G6 同类），当前不生成、不比对，仅登记；③ 14 天复测（M1 `using-superpowers` ≥16 次、M2 覆盖会话 ≥96 等）需等窗口，判据已预注册在维护者侧的会话记录里。



## 3. 建议顺序

按「对读者的误导程度 × 成本」：

1. **G5**（README 事实过期）—— ✅ 已修：三处事实 + 目录结构 + 新增《占位符说明》。
2. **G12**（发布副本与 live 漂移）—— ✅ 已修：已复发布，且 `--check` 现在逐文件比对 live↔发布副本，漂移不再静默。
3. **G10**（脱敏塌缩丢内容）—— ✅ 已修：私有表加规则救回被吃掉的那条陷阱；同形态已门禁化（单规则塌缩 + 跨规则同常量两条判据）。
4. **G13**（只读取证档）—— ✅ 已修并已随本次复发布进包。
5. **G11**（`scripts/` 绕过门禁）—— ✅ 已修：只跳过仓根第一层，覆盖 23→25。
6. **G1**（质量路径）—— ✅ 已按方向 ① 执行（2026-09-13 迁入 `test-driven-development`）；根因定位仍无包内技能，属"低优先、无实测缺口证据"。
7. **G6**（规则口径）—— ⬜ 未动：先做 ③（声明分工），再决定是否落进技能。
8. **G2**（`github` 对比句）/ **G4**（`spike` 引用 `sketch`）—— ✅ 已修：改成自足表述，包内已无指向包外技能的派发式引用。
9. **G8**（内部引用可解析自检）—— ✅ 已做（2026-09-13）：落地为 O5 门禁（悬空引用 + 四方计数比对，ERROR 级），并顺带修掉它自己暴露出的两个假阴性/自指坑。
10. **G18**（运行期触达层缺失）—— ✅ 2026-09-16 落地：描述症状化 + `workflow-trigger` 条件注入 + 只读审计脚本；同题 A/B 实测触达 1/4 → 4/4、显式并行 1 路 → 3 路同批。**复测判据已预注册**（需等 14 天窗口）。
11. **G3 / G7 / G9** —— G3、G7 ⬜ 低影响或属长期设计取向（G3 已有降级说明，按原推荐「什么都不做」）；G9 🟡 五项里「README 的《目录结构》补 `active/<skill>/scripts/`」已在 G5 一并修掉，其余（CHANGELOG、版本注记）未动。

## 4. 尚待定夺的两个问题

- **Q1 判定标准**：所谓「完整工作流」，是要求**包能独立跑通全链**（读者只靠包即可），还是**包＝执行面、私有设计文档＝原理面**？这直接决定 G1 / G6 / G7 往哪边补。
- **Q2 三个被引用的包外技能**（`github`、`deliverable-checker-suite`、`sketch`）：是**该进包**，还是**该把引用改自足**？二者都不做，就会长期留在 G2/G3/G4。

## 5. 复验命令

在仓库根执行（Windows + git-bash；把 `G` 换成你的本机路径）：

```bash
G="<path-to>/skill-grimoire"
cd "$G" && python scripts/publish.py --check
# 期望：✓ 门禁通过：12 个技能；文档计数一致；…（rc=0）
# 该门禁已覆盖：脱敏/编码/frontmatter/行尾 + **文档计数四方一致** + 包内无悬空引用。
# 计数与引用检查都在门禁里，所以本节的旧手抄命令已删除——手抄正是本批出过的错
# （第一次改动就在 §2 漏了一处旧计数声明（N=9，实际 10），且旧命令的 grep 只拦「八个技能」这一种词形）。

# 宿主侧的「一个技能只有一份可发现副本」复验（门禁看不到宿主技能树，须手跑）：
find ~/.codex/skills/active "$LOCALAPPDATA/hermes/skills" -name SKILL.md -path '*test-driven-development*' | wc -l
# 期望 1（另一侧那份以 SKILL.md.disabled 退役；.disabled 不参与宿主发现）
```

两个根文档的不变量（重复长行 / 行尾）与占位符自扫：

```bash
python - <<'PY'
import collections, os, re
for p in ("README.md", "ROADMAP.md"):
    b = open(p, "rb").read()
    ls = b.decode("utf-8").split("\n")
    dup = [l for l, n in collections.Counter(x for x in ls if len(x) >= 40).items() if n > 1]
    print(p, "CRLF" if b"\r\n" in b else "LF", "重复长行:", dup or "无")
c = collections.Counter()
for dp, dn, fn in os.walk("."):
    if ".git" in dp.split(os.sep):
        continue
    for f in fn:
        try:
            t = open(os.path.join(dp, f), encoding="utf-8").read()
        except Exception:
            continue
        c.update(re.findall(r"<[A-Z][A-Z0-9_]{2,}>", t))
print("占位符:", dict(c) or "无")
PY
```

设计内**只应有** `<PROJECT_DIR>` / `<NOTE_VAULT>` / `<OBSIDIAN_VAULT>` 三个全大写占位符；冒出第四个 = 有人手写了占位符或替换表漏项，要查。

**这条自扫的数字在克隆侧与维护者侧不同，别当成不变量**：维护者侧当前各 5 处（技能正文示例 1 + 脱敏替换表 2 + README 说明表 1 + 本节 1），但其中「脱敏替换表 2」来自被 `.gitignore` 忽略的本机私有表——克隆者跑同一条命令只会得到各 3 处。判定标准是**占位符的种类**（只应有那三个）与「仓内文本里没有第四种」，不是出现次数。

**索引注入成本（口径）**：Codex 技能面逐条 `name` + `description` + 固定路径开销。2026-09-12 实测 **45 条 / 11,217 字符 ≈ 2.8k tokens/轮**；早前数字附录记的 46 条 / 18,340 字符 ≈ 4.6k 已过期，不要继续引用。**本仓的复验命令不覆盖这个口径**，增删技能后需要维护者侧重新测。

悬空引用（G1–G4）自查：

```bash
python - <<'PY'
import os, re
G = os.environ.get("SKILL_GRIMOIRE", ".")
pkg = {d for d in os.listdir(G + "/active") if os.path.isdir(os.path.join(G, "active", d))}
for sk in sorted(pkg):
    for r, _, fs in os.walk(os.path.join(G, "active", sk)):
        for f in fs:
            if not f.endswith((".md", ".yaml")):
                continue
            t = open(os.path.join(r, f), encoding="utf-8", errors="ignore").read()
            names = set(re.findall(r"`([a-z0-9][a-z0-9-]{2,})/(?:references|scripts|templates|assets)/", t))
            names |= {x.strip() for m in re.findall(r"related_skills:\s*\[([^\]]*)\]", t) for x in m.split(",") if x.strip()}
            for n in sorted(names):
                if n != sk and n not in pkg:
                    print("悬空引用:", sk, "->", n)
PY
```

## 6. 维护纪律

- 本文件在 `scripts/publish.py` 的 `KEEP_TOP` 白名单内，**不会被发布流程删除**（该流程会清掉根目录里不在白名单的条目）。
- 发布流程的脱敏门禁会扫描本仓库的**所有文本文件**（含本文件）：写入时不得包含私人路径、私人项目名、实名、凭据；命中即 fail-closed、拒绝推送。
- 新增/删除技能、或改 `README.md` 之后，必须复跑 §5 并更新 §2 与「最后核对」日期。
- 改 `scripts/publish.py` **必须带测试**：`python scripts/test_publish.py -v` 必须全绿；**新增用例必须做过变异检验**（把被测逻辑改坏，证明该用例变红）——否则用例可能什么都没测。
- 门禁的计数规则按**字面**匹配「包内 N 个技能」：叙述性引用旧计数时写成 `N=9`，不要复述声明句式，否则维护者自己的说明文字会把门禁拦下。
